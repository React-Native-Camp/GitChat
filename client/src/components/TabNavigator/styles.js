import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  tabNavigator: {
    height: 70,
    paddingBottom: 10,
    paddingTop: 10,
  },
  labelStyle: {
    fontSize: 12,
    fontWeight: 'bold',
  },
});
